# V0.1 Reader Gate Report

Version: V0.1 Reader

Implementation Status: COMPLETE

Validation Status: BLOCKED

## Completed

- Deterministic `.xlsx` workbook and Sheet metadata scanner.
- Ordinary-cell, merged-cell, dimensions, style-signature, row-height, and column-width extraction.
- Ascending and descending contiguous U-axis candidate detection without fixing one rack height.
- Rack candidate detection for dual-axis, shared-axis, and single-axis edge layouts.
- Device candidate extraction with separate Rack, Device, and Placement candidate records.
- Start U, End U, Height U, Source Sheet, Source Range, confidence, evidence, and ambiguity issues.
- Deterministic candidate IDs based on source coordinates; these are not V0.3 stable identities.
- JSON output through `racktool analyze` without modifying the source workbook.
- Boundary coverage for malformed XLSX, non-XLSX, short integer runs, unresolved axes, ascending U,
  shared axes, edge racks, duplicate titles, overlapping placements, 1U, and multi-U devices.
- Golden Sample regression framework under `tests/regression/` and documented sample contract.
- Private V0.1 review workbook with gate overview, all 163 inventory reconciliation rows, all 90 rack
  candidates, all 711 device/placement candidates, all 11 analysis issues, filters, and controlled
  manual-decision fields. The workbook is Git-ignored and does not itself constitute approved expected
  results.

## Tests Run

- command: `.venv/bin/pytest`
- result: 14 passed, 1 skipped
- skipped: no sanitized, manually confirmed Golden Sample case is installed
- command: `.venv/bin/ruff check .`
- result: PASS
- command: `.venv/bin/mypy src`
- result: PASS, strict mode
- command: `racktool analyze <private-rack-workbook>` through a captured CLI process
- result: PASS; output is valid deterministic JSON and the source SHA-256 is unchanged
- command: private review workbook key-range inspection, formula-error scan, and five-Sheet render
- result: PASS; computed totals are 163/90/711/11, formula-error matches are zero, and all five review
  Sheets render without clipped key headers or broken note columns

## Golden Samples

### private-layout-a

- sample: first real layout Sheet in the private rack workbook
- expected: not yet manually confirmed
- actual: 12 U-axis candidates, 6 rack candidates, 42 device candidates, 42 placements, and one
  duplicate-title ambiguity group
- result: BLOCKED — actual output exists, but no approved expected JSON exists

### private-layout-b

- sample: second, materially different real layout Sheet in the private rack workbook
- expected: not yet manually confirmed
- actual: 84 U-axis candidates, 84 rack candidates, 669 device candidates, 669 placements, nine
  duplicate-title ambiguity groups, and one non-text device ambiguity group
- result: BLOCKED — actual output exists, but no approved expected JSON exists

### private-inventory-reference

- sample: two-column private asset inventory workbook
- expected: used only as an independent reconciliation reference; it is not a rack-layout Golden Sample
- actual: 163 data rows; 123 placement-and-name matches, 2 placement matches with different names,
  36 placements not matched, and 2 placement strings not parsed
- result: BLOCKED — discrepancies require human classification as workbook error, inventory error,
  intentional placeholder, ignored label, or Parser defect

## Regression

- total collected: 15
- passed: 14
- failed: 0
- skipped: 1 Golden Sample case because no sanitized, manually confirmed expected JSON is installed

## Known Limitations

- Text in a detected device area remains a candidate; reserved, empty, placeholder, color-semantic,
  front/back, and non-device labels are not silently classified.
- Non-text values inside a device area are retained at lower confidence and require human classification.
- Duplicate rack titles are reported but not auto-corrected.
- Candidate IDs are source-coordinate identifiers and do not satisfy V0.3 move/re-scan identity rules.
- The private rack workbook contains two layouts in one file; the inventory workbook is not a second
  rack-layout workbook.

## Blocking Items

- Complete the private V0.1 review workbook and approve or correct Rack, Device, Placement, and
  ignored-label expectations for both real layouts.
- Classify the 40 non-exact inventory reconciliation rows, and sample-check the 123 automatic matches,
  instead of choosing one source silently.
- Produce sanitized Golden Sample packages with manually confirmed `expected.json`, or explicitly approve
  an equivalent private Golden workflow with durable expected results.

## Gate Decision

BLOCKED — do not proceed to V0.2.
