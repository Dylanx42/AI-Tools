# V0.5 Local GUI Gate Report

Version: V0.5 Local GUI

Implementation Status: COMPLETE

Validation Status: PASS

## Completed

- Qt-free `GuiSession` cockpit logic that only calls RackCore.
- Open workbook / open existing SQLite project.
- Device list, rack list, occupancy view, mapping view, and conflict view.
- Plan a Rack/U move and show conflicts before write-back.
- Safe sync through the existing backup / temp-file / validate path.
- JSON export.
- Backup listing and restore.
- Optional PySide6 window and `racktool gui` entry; GUI widgets are not required for tests.

## Tests Run

- command: `.venv/bin/pytest`
- result: PASS, including new `tests/unit/test_gui_session.py`
- command: `.venv/bin/ruff check .`
- result: PASS
- command: `.venv/bin/mypy src`
- result: PASS, strict mode
- command: V0.1 through V0.4 regression
- result: remains green

## Acceptance

1. Open workbook and list devices/racks/mappings: PASS
2. Occupied-target preview is refused: PASS
3. Move, export, and restore backup: PASS
4. GUI does not copy Parser/Sync logic: PASS
5. Agent runtime is not embedded: PASS

## Known Limitations

- Drag and drop is deferred.
- The occupancy view is a U table, not a visual rack diagram.
- PySide6 is an optional extra; headless CI can validate the cockpit session without Qt.

## Gate Decision

PASS — V0.6 Unknown Layout Workflow may begin. V0.1 through V0.5 regression must remain green.
