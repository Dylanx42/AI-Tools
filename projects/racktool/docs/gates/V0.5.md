# V0.5 Local GUI Gate Report

Version: V0.5 Local GUI

Implementation Status: COMPLETE — usability redesign candidate

Automated Validation Status: **AUTOMATED PASS — RackCore + GuiSession + PySide6 headless**

Manual Validation Status: **MANUAL VALIDATION PENDING — redesigned macOS GUI user acceptance and
Windows GUI inspection.**

Current integrated evidence: [V0.5 Integrated Audit](V0.5-integrated-audit.md).

## 2026-09-15 usability remediation

The earlier table-centric PySide6 window passed its headless contract but was rejected during macOS
review as not usable enough for ordinary operators. That result was treated as a real V0.5 defect,
not as a cosmetic preference. The window was replaced with the approved workflow:

- overview, rack, device, and exception pages;
- searchable human-readable rack list without exposing raw IDs in the primary UI;
- one selected-rack elevation with bounded scene contents instead of rendering the whole workbook;
- friendly device detail with IDs and source ranges collapsed under Technical Information;
- a move drawer with target rack/start-U selection and live RackCore conflict preview;
- a pending-change queue that does not modify XLSX;
- one global **Sync Changes** action for committing all queued device moves;
- a RackCore batch plan committed through the existing backup, temporary-write, reload-validation,
  atomic-replace, and workbook/SQLite rollback path;
- device and exception tables capped at 100 visible rows to avoid the former 710-by-8 Qt
  accessibility/performance failure mode.

The GUI imports no `openpyxl` and contains no independent workbook writer. Drag and drop remains
deferred; the supported V0.5 move interaction is select, preview, queue, review, and sync.

The follow-up export workflow adds a primary **Export spreadsheet** action while retaining JSON in
the File menu. RackCore, not the GUI, creates a separate `.xlsx` with exactly two reader-facing
sheets: a standardized rack diagram and a filterable device-position table. It preserves embedded
line breaks, keeps every original source rack row together instead of wrapping after a fixed count,
preserves source workbook Sheet order and rack column order, draws complete merged-cell outlines,
treats formula-like device labels as literal text, refuses the source workbook as a destination,
and saves through temporary write, reload validation, and atomic replace. Pending moves are
explicitly excluded until they are synced.

The follow-up operator review was implemented and independently rechecked:

- the pending/exception metric and a dedicated button both open the handling page;
- the rack sidebar has an explicit draggable splitter, full-name horizontal fallback, and source,
  natural-name, or occupancy sorting;
- Excel line breaks remain visible in rack elevation, device table/detail, and pending changes;
- issues are grouped in plain Chinese with an in-page rescan action, while raw codes stay under a
  collapsed technical-detail control; the main table now shows the exact source Sheet and cell/range
  for each affected position, or explicitly identifies a project-level issue when no single cell exists;
- the device page has rack, status, height, and ordering filters;
- the overview contains a read-only, Sheet-scoped source-layout view produced through
  `GuiSession` and RackCore scanning rather than direct GUI workbook access.

Independent macOS visual QA found and fixed one additional regression that headless assertions had
missed: the clickable overview exception card collapsed to a blank strip. The card now has the same
usable height as the other overview cards, and a regression assertion protects it. The splitter hit
area, rack-item sizing, translated non-active statuses, and unexpected sync-exception handling were
also hardened before the final rerun.

## Tests run

- full RackTool suite: **159 passed, 0 failed, 0 skipped**;
- integration suite: **89 passed**;
- private real-Golden regression: **3 passed**;
- Safe Sync/write-back suite: **29 passed**;
- dedicated presentation, `GuiSession`, and PySide6 headless GUI suite: **37 passed**;
- managed-storage retention and cleanup suite: **3 passed**;
- dedicated Excel export behavior: **9 passed** across RackCore/CLI/GuiSession/window;
- Ruff: **PASS**;
- strict mypy over all RackTool source: **PASS**;
- CLI inspect/analyze/project-import/project-rescan/export smoke: **PASS**;
- real private Golden workbook copy, offscreen PySide6 load: **90 racks / 710 devices loaded**;
- bounded rendering check: **100 visible device rows and only the selected rack rendered**;
- source-safety check after load and move preview: temporary workbook remained byte-identical to the
  private source.
- real private-workbook copy export: **90 racks / 710 devices**, two sheets reloaded and rendered;
  private output remains Git-ignored and is not an additional Golden Sample.

Focused counts overlap the 159-test full suite and must not be added together. The private workbook
runtime smoke uses the existing ignored Golden source; it does not create or claim another Golden.

## Acceptance

1. Open workbook / existing SQLite project: **AUTOMATED PASS**
2. Browse racks, visual elevation, devices, and exceptions: **AUTOMATED PASS**
3. Search without materializing all 710 devices in the table: **AUTOMATED PASS**
4. Preview occupied targets and pending-plan overlaps: **AUTOMATED PASS**
5. Queue a move without changing XLSX: **AUTOMATED PASS**
6. Commit multiple queued moves in one Safe Sync transaction: **AUTOMATED PASS**
7. GUI uses RackCore and has no direct Excel writer: **AUTOMATED PASS**
8. Export JSON and restore backup: **AUTOMATED PASS**
9. Keep GUI project state/backups out of the source directory with bounded cleanup: **AUTOMATED PASS**
10. Show each issue's affected source Sheet and cell/range in the operator-facing table:
   **AUTOMATED PASS**
11. Export rack diagram + filterable device-position XLSX without modifying the source, fixed-count
   wrapping, or incomplete merged-cell outlines:
   **AUTOMATED PASS**
12. macOS native window launch with a private-workbook copy: **RUNTIME SMOKE PASS**
13. Redesigned macOS GUI usability acceptance by the user: **MANUAL VALIDATION PENDING**
14. Windows PySide6 GUI inspection: **MANUAL VALIDATION PENDING**
15. Microsoft Excel/WPS open-and-print review of the new export: **MANUAL VALIDATION PENDING**

## Gate decision

**AUTOMATED PASS.** The redesigned macOS window is currently open for user acceptance. Runtime
launch is not a substitute for human usability approval, so macOS and Windows manual validation are
not marked PASS here.
