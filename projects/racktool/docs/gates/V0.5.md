# V0.5 Local GUI Gate Report

Version: V0.5 Local GUI

Implementation Status: COMPLETE

Automated Validation Status: **AUTOMATED PASS — RackCore + GuiSession/headless**

Manual Validation Status: **MANUAL VALIDATION PENDING — macOS/Windows GUI 人工检查。**

Current integrated evidence: [V0.5 Integrated Audit](V0.5-integrated-audit.md).

## Completed

- Qt-free `GuiSession` cockpit logic that only calls RackCore.
- Open workbook / open existing SQLite project.
- Device list, rack list, occupancy view, mapping view, and conflict view.
- Plan a Rack/U move and show conflicts before write-back.
- Safe sync through the existing backup / temp-file / validate path.
- JSON export.
- Backup listing and restore.
- Optional PySide6 window and `racktool gui` entry exist, but the Qt widget window was not executed in
  the audit environment because PySide6 is not installed.

## Tests Run

- command: independent post-fix full suite
- result: 112 passed, 0 failed, 0 skipped
- command: headless GUI + Profile lifecycle focused suite
- result: 9 passed
- command: `.venv/bin/ruff check .`
- result: PASS
- command: `.venv/bin/mypy src`
- result: PASS, strict mode
- command: V0.1 through V0.4 regression
- result: remains green

## Acceptance

1. Open workbook and list devices/racks/mappings: PASS
2. Occupied-target preview is refused: PASS
3. Move, export, and restore backup: PASS
4. GUI does not copy Parser/Sync logic: PASS
5. Agent runtime is not embedded: PASS
6. macOS/Windows PySide6 widget runtime: MANUAL VALIDATION PENDING

## Known Limitations

- Drag and drop is deferred.
- The occupancy view is a U table, not a visual rack diagram.
- PySide6 is an optional extra and was not installed in the audit environment; headless tests do not
  constitute macOS/Windows Qt widget validation.

## Gate Decision

AUTOMATED PASS — RackCore + GuiSession/headless. This does not mark the pending macOS/Windows GUI
manual validation as PASS; V0.1 through V0.5 regression must remain green.
