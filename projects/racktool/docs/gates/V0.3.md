# V0.3 Identity & Mapping Gate Report

Version: V0.3 Identity & Mapping

Implementation Status: COMPLETE

Original Gate Validation Status: PASS

Current Integrated Automated Status: AUTOMATED PASS — see
[V0.5 Integrated Audit](V0.5-integrated-audit.md).

> 下列 34 项测试是原 V0.3 门禁的历史运行；当前 Identity focused 结果为 17 passed，完整总套件为
> 112 passed / 0 skipped。各子集相互重叠，不得相加。

## Completed

- Stable `rack_id` / `device_id` assignment on first import.
- Placement and SourceMapping records bound to workbook fingerprint, Sheet, and source range.
- SQLite local project state for identity persistence.
- Rack re-scan uses unique name, persisted source range, and unique structural evidence in stages.
- Device text can reuse an identity only when source/range or structural evidence corroborates it;
  conflicting text/range evidence is blocked before range or unique-structure fallback.
- Ambiguous, competing, or insufficient evidence fails closed and is not silently guessed or persisted.
- Display-text changes keep `device_id`.
- Device moves update Placement/Mapping without duplicating the device.
- Device swaps, rename+move, rack rename/relocation/removal/reappearance, and identical re-scans keep
  referentially valid identities or return a blocking conflict.

## Historical Tests Run — original V0.3 gate

- command: `.venv/bin/pytest`
- result: 34 passed, 0 failed, 0 skipped in the original private-Golden gate environment
- command: `.venv/bin/ruff check .`
- result: PASS
- command: `.venv/bin/mypy src`
- result: PASS, strict mode

## Acceptance

1. Rename keeps `device_id`: PASS
2. Move/swap updates Mapping without duplicating or stealing identity: PASS
3. Rack rename, relocation, removal, and reappearance preserve valid references: PASS
4. Ambiguous or uncorroborated identity evidence is refused without persisting a mutated project: PASS
5. Current Identity focused suite: 17 passed
6. V0.1 / V0.2 regression remains green: PASS

## Gate Decision

AUTOMATED PASS — reconfirmed at the integrated V0.5 audit baseline. V0.1, V0.2, and V0.3 regression
must remain green in every later gate.
